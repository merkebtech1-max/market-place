import { PlaceholderPage } from "@/components/layout/PlaceholderPage";
import { T } from "@/il8n/T";

export default function SavedPage() {
  return (
    <PlaceholderPage title={<T k="placeholder.savedTitle" />} body={<T k="placeholder.savedBody" />} />
  );
}
