import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function SellPage() {
  return <PlaceholderPage title="Sell an item" body="The 4-step listing composer (SRS §3.2 FR-L1) isn't built yet." />;
}
