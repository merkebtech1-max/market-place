import { PlaceholderPage } from "@/components/layout/PlaceholderPage";
import { T } from "@/il8n/T";

export default function ServicesPage() {
  return (
    <PlaceholderPage
      title={<T k="placeholder.servicesTitle" />}
      body={<T k="placeholder.servicesBody" />}
    />
  );
}
