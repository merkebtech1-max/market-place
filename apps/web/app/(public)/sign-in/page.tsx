import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function SignInPage() {
  return (
    <PlaceholderPage
      title="Sign in"
      body="Phone + SMS OTP sign-in (SRS §3.1 FR-A1) isn't built yet."
    />
  );
}
