import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function NotificationsPage() {
  return <PlaceholderPage title="Notifications" body="Price drops, offers and moderation updates will show up here." />;
}
