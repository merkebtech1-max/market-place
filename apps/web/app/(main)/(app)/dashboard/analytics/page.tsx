import { PlaceholderPage } from "@/components/layout/PlaceholderPage";
import { T } from "@/il8n/T";

export default function DashboardAnalyticsPage() {
  return <PlaceholderPage title={<T k="placeholder.dashboardAnalyticsTitle" />} />;
}
